'use strict';

/**
 * @ngdoc function
 * @name initApp.controller:InoutsCtrl
 * @description
 * # InoutsCtrl
 * Controller of the initApp
 */
angular.module('initApp')
  .controller('InoutsCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });

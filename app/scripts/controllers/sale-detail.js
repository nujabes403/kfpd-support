'use strict';

/**
 * @ngdoc function
 * @name initApp.controller:SaleDetailCtrl
 * @description
 * # SaleDetailCtrl
 * Controller of the initApp
 */
angular.module('initApp')
  .controller('SaleDetailCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });

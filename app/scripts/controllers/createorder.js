'use strict';

/**
 * @ngdoc function
 * @name initApp.controller:CreateorderCtrl
 * @description
 * # CreateorderCtrl
 * Controller of the initApp
 */
angular.module('initApp')
  .controller('CreateorderCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });

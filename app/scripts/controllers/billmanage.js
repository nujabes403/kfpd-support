'use strict';

/**
 * @ngdoc function
 * @name initApp.controller:BillmanageCtrl
 * @description
 * # BillmanageCtrl
 * Controller of the initApp
 */
angular.module('initApp')
  .controller('BillmanageCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
